import org.junit.jupiter.params.ParameterizedTest;import org.junit.jupiter.params.provider.MethodSource;import java.util.Arrays;import java.util.List;import static org.junit.jupiter.api.Assertions.assertTrue;public class ParameterizedListTest {        @ParameterizedTest    @MethodSource("provideTestData")    void testListSize( List<String> input, int expectedSize) {                System.out.println(input.size() );                assertTrue(input.size() == expectedSize);    }        static List<Object[]> provideTestData() {        return Arrays.asList(                                new Object[]{Arrays.asList("x", "y"), 2},                new Object[]{Arrays.asList("a", "b", "c"), 3},                new Object[]{ Arrays.asList("z"),1}                //  1,2,3        );    }}